h1
    a(href="/dashboard")
        img(src="/res/logo.png", alt="logo")
        | #{title}
    
.manage
    .profile
        img(src="/myaccount/pic").profilePic
        span.username=username
        
        .options
            //- button(onclick='window.location.href="/myaccount"')#myAccount My Account
            form(method="get", action="/myaccount")
                button(type="submit") My Account
            form(method="post", action="/logout")
                button(type="submit") Log Out
